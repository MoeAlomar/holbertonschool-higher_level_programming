-- List all records from the table second_table, ordered by score (top first)

-- Select score and name from second_table and sort by score descending
SELECT score, name
FROM second_table
ORDER BY score DESC;
