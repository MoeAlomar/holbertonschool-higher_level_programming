#!/usr/bin/python3
def print_matrix_integer(matrix=[[]]):
    if len(matrix[[]]) <= 0:
    return None
    for i in range(len(matrix)):

            for j in range(len(matrix[i])):
                if j < len(martix[i]) - 1:
                print("{:d}".format(martix[i][j],end = " "))
                else:
                    print("{:d}".format(martix[i][j],end = ""))
        print()
